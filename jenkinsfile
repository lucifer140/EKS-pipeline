pipeline {
    agent any

    stages {
        stage('deploy') {
            steps {
                sh '''
                ls
                kubectl apply -f deployment.yaml
               '''
            }
        }
        
    }
}
